

cd ~/workspace/github/docker-test/src

-----------------------------------

port

php: 4000:80
vite: 4173:4173
mysql: 4306:3306
rails: 3000

-----------------------------------

PHP


cd ~/workspace/github/docker-test/src

docker compose build

docker compose down

docker compose up

docker compose up --build

http://localhost:4000

----

Rails

cd ~/workspace/github/docker-test/rails


ローカル起動

foreman start -f Procfile.dev

bin/rails s

bin/vite dev


docker起動

docker compose up

docker compose exec web bundle exec foreman start -f Procfile.docker.dev

docker compose exec web bin/rails server -b 0.0.0.0

docker compose exec web bin/vite dev


http://localhost:3000

----

Laravel

cd ~/workspace/github/docker-test/laravel

composer dev

-----------------------------------

セットアップ


PHP

cd ~/workspace/github/docker-test/src

docker compose up


cp .env.example .env


docker compose exec php composer install

docker compose exec node npm ci

----

ブロックチェーン

cd ~/workspace/github/docker-test/bc

docker compose up

docker compose exec node npm ci

----

Rails

cd ~/workspace/github/docker-test/rails


ローカル起動

bundle install

npm ci

cp _env.example .env

bin/rails db:create

bin/rails db:migrate


docker起動

docker compose up

docker compose exec web bin/rails db:create

docker compose exec web bin/rails db:migrate

----

Laravel

cd ~/workspace/github/docker-test/laravel

ローカル起動

npm ci

-----------------------------------

ビルド

docker compose run --rm node npm run build

.env
APP_ENV
変更

-----------------------------------

MYSQL

docker compose exec mysql mysql -uapp -p

secret

use app

---

docker compose exec \
  -e MYSQL_PWD=secret \
  mysql mysql -uapp app

---

テーブル作成

docker compose exec -T \
  -e MYSQL_PWD=secret \
  mysql mysql -uapp app < database/sqls/20260107_0000_create_users.sql

---

テストデータ追加

docker compose exec -T \
  -e MYSQL_PWD=secret \
  mysql mysql \
  --default-character-set=utf8mb4 \
  -uapp app <<'SQL'
INSERT INTO users (name) VALUES
('テスト１'),
('テスト２'),
('テスト３');
SQL

-----------------------------------

ブロックチェーン

cd ~/workspace/github/docker-test/bc

docker compose up


仮想ブロックチェーン起動

docker compose exec node npm run dev

docker compose exec node npx hardhat node

動作確認

docker compose exec node npx hardhat run scripts/deploy.js --network localhost

docker compose exec node npx hardhat run scripts/nft-test.js --network localhost


-----------------------------------

Kubernetes

cd ~/workspace/github/docker-test/k8s


kubectl apply -f .

kubectl delete -f .

http://localhost:30080/

----

ログ

kubectl logs -f deployment/nginx

kubectl logs -f deployment/php

kubectl logs -f -l app

----

接続

kubectl exec -it deployment/nginx -- sh

kubectl exec -it deployment/php -- sh

----

Kubernetesクラスタのサーバー一覧を取得

kubectl get nodes

実際に動いてるアプリ一覧を見る

kubectl get pods

Service一覧を見る

kubectl get svc

-----------------------------------

テスト

cd ~/workspace/github/docker-test/src

docker compose exec php ./vendor/bin/phpunit
